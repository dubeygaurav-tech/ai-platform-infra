apiVersion: v1
kind: Service
metadata:
  name: vllm-service
  namespace: ai-workloads
spec:
  selector:
    app: vllm-inference
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP